apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "itsm-dev-platform.fullname" . }}-test-connection"
  labels:
    {{- include "itsm-dev-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "itsm-dev-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
